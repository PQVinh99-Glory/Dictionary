# Dictionary Cloudflare Real Deploy

Bản này dành cho trường hợp:

- máy công ty không có Node;
- app đã chạy tại `https://dictionary-dnw.pages.dev/`;
- muốn test trực tiếp Pages Functions + R2 Binding;
- GitHub là source deploy;
- giữ Worker cũ làm fallback.

Đọc `DEPLOY_REAL_CLOUDFLARE.md` và làm đúng thứ tự.
